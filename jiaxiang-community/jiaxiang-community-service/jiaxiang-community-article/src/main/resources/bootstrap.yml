spring:
  application:
    name: jiaxiang-article
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: localhost:8848
      username: 'nacos'
      password: 'nacos'
      discovery:
        namespace: de2f54a0-2c19-4cbc-87ca-3d9b873c208e
      config:
        refresh-enabled: true
        namespace: de2f54a0-2c19-4cbc-87ca-3d9b873c208e
        group: jiaxiang_article_group
        file-extension: yaml
  config:
    import:
      - nacos:${spring.application.name}-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}?refreshEnabled=true
