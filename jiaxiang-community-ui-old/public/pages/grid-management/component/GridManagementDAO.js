class GridManagementDAO {
    community;
    icon;
    meta;
    constructor(community, icon, meta) {
        this.community = community;
        this.icon = icon;
        this.meta = meta;
    }
}
export default GridManagementDAO;
